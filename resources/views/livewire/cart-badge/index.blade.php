<div>
    <div>
        @if ($count > 0)
            <span
                class="absolute -top-2 -right-3 bg-red-500 text-white text-xs rounded-full size-5 flex items-center justify-center">
                {{ $count }}
            </span>
        @endif
    </div>
</div>
